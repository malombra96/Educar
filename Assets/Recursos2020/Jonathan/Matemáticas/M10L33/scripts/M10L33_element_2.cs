using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class M10L33_element_2 : MonoBehaviour
{
    // Start is called before the first frame update

    public M10L33_player player;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        ///transform.Rotate(new Vector3(transform.rotation.x,player.transform.rotation.y, transform.rotation.z));
    }
}
