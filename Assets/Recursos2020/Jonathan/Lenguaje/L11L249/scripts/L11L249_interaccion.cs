using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class L11L249_interaccion : MonoBehaviour
{
    // Start is called before the first frame updatep
    public Text texto;
    void Start()
    {
        texto.text = "100%";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
